{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Server Debug",
      "type": "debugpy",
      "request": "launch",
      "module": "server",
      "args": ["--server-type", "threaded", "--port", "8002"],
      "justMyCode": true,
      "cwd": "${workspaceFolder}"
    }
  ]
}
